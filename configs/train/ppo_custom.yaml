# rollout 길이, 저장 주기, 전체 iteration을 수정하는 사용자 학습 preset입니다.
runner: OnPolicyRunner
num_steps_per_env: 24
max_iterations: 1000
save_interval: 100
empirical_normalization: false
clip_actions: 1.0
policy: ../policy/mlp_custom.yaml
algorithm: ../algorithm/ppo_custom.yaml
