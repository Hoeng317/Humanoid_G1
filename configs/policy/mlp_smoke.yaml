# Small network for pipeline validation only. It is not a walking-policy preset.
init_noise_std: 1.0
actor_obs_normalization: false
critic_obs_normalization: false
actor_hidden_dims: [128, 64]
critic_hidden_dims: [128, 64]
activation: elu
