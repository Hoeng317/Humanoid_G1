"""ACCAD-to-G1 preprocessing pipeline.

This package deliberately lives beside the read-only ACCAD data.  It may read
the parent humanoid_G1 project, but every generated artifact is kept below the
``ACCAD/_g1_pipeline`` directory.
"""

__version__ = "0.3.0"
